# AppInspector

Android-приложение для инспекции установленных приложений на устройстве. Позволяет просматривать детальную информацию о каждом приложении, включая контрольные суммы APK.

## Функциональность

- 📱 Просмотр всех установленных приложений
- 🔍 Детальная информация о каждом приложении:
  - Название и иконка
  - Имя пакета
  - Версия (имя и код)
  - Контрольная сумма APK (SHA-256)
  - Тип приложения (системное/пользовательское)
- 🚀 Запуск приложений напрямую из списка
- 🎨 Material Design 3 UI
- 🌍 Поддержка русского языка

## Технический стек

- **Язык**: Kotlin
- **UI**: Jetpack Compose
- **Архитектура**: MVI (Model-View-Intent) + Clean Architecture
- **DI**: Hilt
- **Навигация**: Jetpack Navigation Compose (Type-safe)
- **Асинхронность**: Kotlin Coroutines + Flow
- **Логирование**: Timber
- **Тестирование**: JUnit, MockK, Compose UI Testing

## Архитектура

Проект следует принципам Clean Architecture и разделен на слои:

- **presentation**: ViewModels, UI компоненты (MVI)
- **domain**: Use Cases, бизнес-логика
- **data**: Repositories, data sources

### MVI Pattern

- **Model**: `State` (UI состояние)
- **View**: Jetpack Compose
- **Intent**: `Intent` (пользовательские действия)

## Основные компоненты

### UI Screens
- `AppListScreen` — список всех приложений с группировкой по алфавиту
- `SingleAppScreen` — детальная информация о приложении

### ViewModels
- `AppsViewModel` — управление списком приложений
- `SingleAppViewModel` — управление деталями приложения

### Repositories
- `AllAppsRepository` — получение списка всех приложений
- `AppDetailsRepository` — получение детальной информации о приложении

### Use Cases
- `LaunchAppUseCase` — запуск приложения с обработкой всех крайних случаев

## Требования

- Android 7.0 (API 24) или выше
- Разрешение `QUERY_ALL_PACKAGES` для полного доступа к списку приложений

## Сборка

```bash
./gradlew assembleDebug
```

APK будет создан в `app/build/outputs/apk/debug/app-debug.apk`

## Тестирование

### Unit тесты
```bash
./gradlew testDebugUnitTest
```

### UI тесты (требуется подключенное устройство или эмулятор)
```bash
./gradlew connectedDebugAndroidTest
```

## Структура проекта

```
app/
├── src/
│   ├── main/
│   │   ├── java/com/example/appinspector/
│   │   │   ├── data/          # Data layer
│   │   │   ├── domain/        # Domain layer
│   │   │   ├── presentation/  # Presentation layer (MVI)
│   │   │   ├── ui/            # Composables, themes
│   │   │   └── navigation/    # Navigation graph
│   │   └── res/               # Resources
│   ├── test/                  # Unit tests
│   └── androidTest/           # UI tests
└── build.gradle.kts
```

## Особенности реализации

- **Thread-safe state updates**: Использование `MutableStateFlow.update` для атомарных обновлений
- **SavedStateHandle**: Автоматическое сохранение состояния при process death
- **Retry policy**: Автоматические повторные попытки для I/O операций
- **Type-safe navigation**: Использование `kotlinx.serialization` для безопасной навигации
- **Structured error handling**: `sealed interface AppError` для типизированных ошибок
- **Resource management**: `ResourceProvider` для доступа к строкам в ViewModel

## Лицензия

Этот проект создан в рамках тестового задания для Doctor Web.

## Автор

Artemy Velichko
